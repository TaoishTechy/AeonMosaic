# AeonMosaic runtime dependencies
# Pinned to the minimum compatible versions for reproducible builds.

# Core numerical / symbolic stack
numpy>=1.24,<3.0
scipy>=1.10,<2.0
sympy>=1.12,<2.0
networkx>=3.0,<4.0

# Config & utilities
pyyaml>=6.0,<7.0

# Optional: ROS 2 bridge (uncomment on real install)
# rclpy>=4.0
# rosidl-runtime-py>=4.0

# Mesh / IoT (optional extras)
# paho-mqtt>=1.6
# RadioLib>=1.0  ; platform_system == "Linux"

# Testing
pytest>=7.0,<9.0
pytest-cov>=4.0,<6.0

# Linting (dev only)
flake8>=6.0
black>=23.0
mypy>=1.5
